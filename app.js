const $=s=>document.querySelector(s);const main=$('#main');let current=-1,timers=[],completed=new Set(),s={};
const sources={stroop:['https://faculty.washington.edu/chudler/words.html','ワシントン大学：Stroop effect'],frame:['https://www.nobelprize.org/uploads/2018/06/kahnemann-lecture.pdf','Kahneman：Nobel Lecture（2002）'],contrast:['https://www.exploratorium.edu/snacks/color-contrast','Exploratorium：Color Contrast'],memory:['https://psychclassics.yorku.ca/Miller/','Miller（1956）：短期記憶とチャンク'],lines:['https://michaelbach.de/ot/sze-muelue/','Michael Bach：Müller-Lyer Illusion'],monty:['https://www.stat.berkeley.edu/~aldous/Real-World/Monty-Hall.html','UC Berkeley・David Aldous：Monty Hall']};
const exhibits=[{title:'色のトリック',en:'COLOR ILLUSION',category:'錯視',color:'var(--purple)',time:'約1分',desc:'同じはずなのに、違って見える。\nあなたの「見えた」を疑おう。',intro:'真ん中の2つの四角。明るく見えるのはどちら？ まずは直感で選んでみよう。',visual:'<div class="ring"></div><div class="ring"></div>'},{title:'脳の交通渋滞',en:'STROOP CHALLENGE',category:'注意',color:'var(--pink)',time:'約2分',desc:'読めるのに、答えられない？\n色と言葉のスピードバトル。',intro:'文字の「意味」ではなく、文字が塗られた「色」をタップ。一致する問題と食い違う問題を、各8問試します。',visual:'<div class="miniword">あ<span>お</span></div>'},{title:'記憶のポケット',en:'MEMORY POCKET',category:'記憶',color:'var(--green)',time:'約2分',desc:'一瞬の数字、いくつ覚えられる？\n記憶を助けるまとめ方を発見。',intro:'6桁の数字が1桁ずつ現れます。順番どおりに覚えよう。メモを使わず、気軽に挑戦してね。',visual:'<div class="nums"><i>3</i><i>7</i><i>?</i></div>'},{title:'ことばの値札',en:'CHOICE LAB',category:'選択',color:'var(--orange)',time:'約1分',desc:'選んだのは、あなた。それとも…？\nことばを変えると、魅力も変わる。',intro:'同じ価格のチケットを選ぶとしたら？ 表示を読んで、魅力的に感じる方を選んでみよう。',visual:'<div class="framevis"><b>80%</b><b>20%</b></div>'},{title:'直感の3つの扉',en:'PROBABILITY PLAY',category:'確率',color:'var(--blue)',time:'約2分',desc:'扉を変える？ そのままにする？\n直感と確率の意外なすれ違い。',intro:'3つの扉のうち、当たりは1つ。まず1つ選ぼう。案内役は当たりを知っていて、残りから必ずハズレを1つ開けます。',visual:'<div class="doors"><i>1</i><i>2</i><i>3</i></div>'},{title:'伸び縮みする線',en:'SIZE ILLUSION',category:'錯視',color:'var(--mint)',time:'約1分',desc:'ぴったり揃えた、その長さ。\n本当に同じか、測ってみよう。',intro:'スライダーを動かして、下の横線を上の横線と同じ長さに見えるように調整しよう。比べるのは矢羽を除いた横線です。',visual:'<div class="lines"><svg viewBox="0 0 240 24"><path d="M30 12H210 M45 0L30 12 45 24 M195 0L210 12 195 24" fill="none" stroke="currentColor" stroke-width="3"/></svg><svg viewBox="0 0 240 24"><path d="M30 12H210 M15 0L30 12 15 24 M225 0L210 12 225 24" fill="none" stroke="currentColor" stroke-width="3"/></svg></div>'}];
function clearTimers(){timers.forEach(clearTimeout);timers=[]}function later(fn,ms){timers.push(setTimeout(fn,ms))}function updatePass(){$('#passport b').textContent=`${completed.size} / 6`}function finish(){completed.add(current);updatePass()}function btn(label,act,cls='primary'){return `<button class="${cls}" data-act="${act}">${label}</button>`}function actions(v){return `<div class="actions">${v}</div>`}function home(){clearTimers();current=-1;main.innerHTML=`<section class="intro enter"><div><div class="eyebrow">WELCOME TO YOUR MIND</div><h1>その「当たり前」が、<br><em>ひっくり返る。</em></h1></div><p>見る。選ぶ。思い出す。<br>あなたの脳が主役の、6つのアトラクション。<br>気になる展示をタップして、遊びにいこう。</p></section><div class="map-label"><span>EXPLORE THE PARK / 展示マップ</span><span>好きな順番で楽しめます ↘</span></div><section class="grid">${exhibits.map((e,i)=>`<button class="card enter" style="--c:${e.color};animation-delay:${i*.06}s" data-room="${i}"><div class="card-top"><span>0${i+1} / ${e.category}</span><span class="tag">${completed.has(i)?'✓ 体験済み':e.time}</span></div><div class="visual" aria-hidden="true">${e.visual}</div><div class="card-body"><h2>${e.title}</h2><p>${e.desc.replace('\n','<br>')}</p><div class="card-bottom"><span>${e.en}</span><span class="arrow">↗</span></div></div></button>`).join('')}</section><div class="note"><span>✳ 正解するより、驚こう。気づくことが、このパークのゴール。</span><span>音なしでも楽しめます / スマホ対応</span></div>`;window.scrollTo(0,0)}
function room(i){clearTimers();current=i;s={};const e=exhibits[i];main.innerHTML=`<div class="room-head"><button class="back" data-act="home">← 展示マップ</button><span class="eyebrow">0${i+1} / ${e.en}</span></div><div class="room-layout enter"><section class="stage" style="--c:${e.color}" aria-label="体験エリア"><div class="stage-content" id="play"></div></section><aside class="side"><span class="tag">${e.category}・${e.time}</span><h1>${e.title}</h1><p>${e.intro}</p><div class="steps"><span class="active">01 体験</span><span>02 発見</span><span>03 日常へ</span></div><div id="explain" aria-live="polite"></div></aside></div>`;[colorStart,stroopStart,memoryStart,frameStart,montyStart,lineStart][i]();window.scrollTo(0,0)}
function reveal(title,text,tip,key){finish();const src=sources[key];$('#explain').innerHTML=`<div class="reveal"><div class="eyebrow">DISCOVERY!</div><h3>${title}</h3><p>${text}</p><div class="insight"><b>日常に持ち帰ろう</b><p>${tip}</p></div><p class="source">参考：<a href="${src[0]}" target="_blank" rel="noopener">${src[1]}</a></p>${actions(btn('もう一度','retry','secondary')+btn(current===5?'展示マップへ':'次の展示へ →','next'))}</div>`;$('.steps').innerHTML='<span>01 体験 ✓</span><span class="active">02 発見</span><span class="active">03 日常へ</span>'}
function colorStart(){$('#play').innerHTML=`<span class="trial">LOOK CLOSELY</span><h2>どちらの四角が、明るい？</h2><div class="color-field"><div class="color-half"><div class="swatch"></div></div><div class="color-half"><div class="swatch"></div></div></div>${actions(btn('左の四角','color-left','secondary')+btn('同じくらい','color-same','secondary')+btn('右の四角','color-right','secondary'))}`}
function colorAnswer(a){$('.color-field').classList.add('unmask');$('#play .actions').innerHTML=btn('背景を切り替える','color-toggle','secondary');$('#play h2').textContent='実は、2つとも同じ色。';reveal('見えている色は、周りで変わる。',`あなたの答えは「${{left:'左の四角',same:'同じくらい',right:'右の四角'}[a]}」。中央はどちらも同じ灰色（#888888）です。背景の明暗によって、同じ灰色でも違う明るさに感じられます。これは「明るさの同時対比」の体験です。`,'商品の色や写真の印象を比べるなら、背景も揃えてみよう。見え方には個人差や画面の影響もあります。','contrast')}
const names=['赤','青','緑','紫'],colors=['#b42b40','#2155bd','#14734f','#7534ae'];function stroopStart(){$('#play').innerHTML=`<span class="trial">16 TRIALS / COLOR ONLY</span><div class="bigword" style="color:#2155bd">あか</div><p>この例の答えは「青」。<br>文字につられず、塗られた色を選ぼう。</p>${actions(btn('チャレンジ開始','stroop-go'))}`}
function stroopGo(){s={n:0,records:[],order:Array(8).fill(true).concat(Array(8).fill(false)).sort(()=>Math.random()-.5)};stroopTrial()}
function stroopTrial(){if(s.n>=16){stroopResult();return}s.target=Math.floor(Math.random()*4);s.match=s.order[s.n];const word=s.match?s.target:(s.target+1+Math.floor(Math.random()*3))%4;$('#play').innerHTML=`<span class="trial">${s.n+1} / 16</span><h2>文字の「色」は？</h2><div class="bigword" style="color:${colors[s.target]}">${names[word]}</div><div class="color-buttons">${names.map((n,i)=>`<button data-act="stroop-answer" data-value="${i}">${n}</button>`).join('')}</div><p class="feedback">${s.n?'リズムよく、でも正確に。':'色ボタンの場所を確認してから始めよう。'}</p>`;s.start=performance.now()}
function stroopAnswer(v){s.records.push({match:s.match,ms:performance.now()-s.start,ok:v===s.target});s.n++;stroopTrial()}
function stroopResult(){const avg=m=>{let r=s.records.filter(x=>x.match===m&&x.ok);return r.length?Math.round(r.reduce((a,x)=>a+x.ms,0)/r.length):null};let a=avg(true),b=avg(false),score=s.records.filter(x=>x.ok).length;$('#play').innerHTML=`<span class="trial">YOUR RESULT</span><h2>脳の交通量を見てみよう</h2><div class="result-number">${score}<small style="font-size:20px"> / 16 正解</small></div>${[ ['一致',a],['不一致',b]].map(([n,v])=>`<div class="bar-row"><span>${n}</span><div class="bar"><i style="width:${v?Math.min(100,v/Math.max(a||1,b||1)*100):0}%"></i></div><b>${v??'—'}${v?'ms':''}</b></div>`).join('')}<p>正答した試行の平均反応時間</p>`;reveal('「読む」が「色を答える」に割り込む。',`文字の意味と色が食い違うと、色を答える反応が遅くなる傾向が「ストループ効果」です。${a&&b?`今回は不一致が一致より${Math.abs(b-a)}ms${b>=a?'遅く':'速く'}なりました。`:''}短い体験では逆の結果も出ます。端末、練習、偶然の影響があり、能力の診断には使えません。`,'集中したいときは、関係のない文字や通知を視界から外してみよう。無視するにも頭の働きが必要です。','stroop')}
function memoryStart(){$('#play').innerHTML=`<span class="trial">REMEMBER THE ORDER</span><div class="digit">3 · 7 · ?</div><h2>記憶のポケット、開いてる？</h2><p>9桁の数字が、1つずつ現れます。<br>最後に順番どおり入力してください。</p>${actions(btn('数字を見る','memory-go'))}`}
function memoryGo(grouped=false){s.grouped=grouped;s.digits=Array.from({length:9},()=>Math.floor(Math.random()*10)).join('');$('#play').innerHTML='<span class="trial">READY?</span><div class="digit" id="digits">＋</div>';let parts=grouped?[s.digits.slice(0,3),s.digits.slice(3,6),s.digits.slice(6)]:s.digits.split('');parts.forEach((v,i)=>{later(()=>{$('#digits').textContent=v},600+i*(grouped?2100:700));later(()=>{$('#digits').textContent='·'},600+i*(grouped?2100:700)+(grouped?1900:550))});later(()=>{$('#play').innerHTML=`<span class="trial">YOUR TURN</span><h2>覚えた数字を順番に入力</h2><p>分からないところも、気軽に予想してね。</p><input class="answer" id="memory-input" aria-label="覚えた9桁の数字" inputmode="numeric" maxlength="9" autocomplete="off" placeholder="9桁の数字">${actions(btn('答え合わせ','memory-check'))}<p id="input-error" class="feedback" aria-live="polite"></p>`;$('#memory-input').focus()},600+6300)}
function memoryCheck(){const input=$('#memory-input').value.trim();if(!/^\d{9}$/.test(input)){$('#input-error').textContent='数字を9桁入力してください。';return}const score=input.split('').filter((v,i)=>v===s.digits[i]).length;$('#play').innerHTML=`<span class="trial">${s.grouped?'CHUNKING':'YOUR RESULT'}</span><h2>${score===9?'全部、覚えていた！':'思い出した順番を見てみよう'}</h2><div class="result-number">${score}<small style="font-size:20px"> / 9 桁一致</small></div><p>正解：<b>${s.digits.slice(0,3)} ${s.digits.slice(3)}</b><br>入力：${input}</p>${actions(btn('3桁ずつまとめて再挑戦','memory-group','secondary'))}`;reveal('バラバラを、ひとまとまりに。','短時間だけ情報を保つ記憶には限りがあります。「チャンク化」は複数の情報を意味のあるまとまりとして扱う工夫。3桁区切りで再挑戦すると、まとめて覚える感覚を試せます。区切るだけで必ず成績が上がるわけではありません。','電話番号や手順を覚えるとき、意味のあるまとまりに分けてみよう。この短い課題は記憶力の診断ではなく、提示方法を比べる体験です。','memory')}
function frameStart(){$('#play').innerHTML=`<span class="trial">CHOOSE YOUR TICKET</span><h2>くじ付きチケット、買うなら？</h2><p>価格も景品も同じ。表示だけを見て選ぼう。</p><div class="choice-grid"><button class="choice" data-act="frame-a">チケット A<strong>80%</strong><small>の確率で<br>景品が当たる</small></button><button class="choice" data-act="frame-b">チケット B<strong>20%</strong><small>の確率で<br>景品が当たらない</small></button></div>${actions(btn('どちらも同じ','frame-same','secondary'))}`}
function frameAnswer(v){$('#play').innerHTML=`<span class="trial">SAME FACT. DIFFERENT FRAME.</span><h2>条件は、まったく同じでした。</h2><div class="result-number">80%<span style="font-size:28px"> = </span>80%</div><p>当たる確率80% ＝ 当たらない確率20%<br>あなたの選択：${v==='same'?'どちらも同じ':'チケット '+v.toUpperCase()}</p>${actions(btn('表現を入れ替える','frame-flip','secondary'))}<div id="frame-flip" class="insight" style="margin-top:20px">A：当たる80% ／ B：当たらない20%</div>`;s.flip=false;reveal('事実が同じでも、印象は揺れる。','同じ内容でも、得られる面と失う面のどちらを強調するかで、評価や選択が変わることがあります。これが「フレーミング効果」。今回は仕組みを見比べるデモで、あなたが影響されたと判定する実験ではありません。','「成功率」と「失敗率」、「月額」と「年間総額」。別の言い方に置き換えてから選ぶと、条件を比較しやすくなります。','frame')}
function montyStart(){s={prize:Math.floor(Math.random()*3),picked:null,opened:null};$('#play').innerHTML=`<span class="trial">ONE PRIZE. THREE DOORS.</span><h2>当たりは、どの扉？</h2><div class="door-row">${[0,1,2].map(i=>`<button class="door" data-act="door" data-value="${i}" aria-label="扉${i+1}を選ぶ">${i+1}</button>`).join('')}</div><p>扉を1つタップしてください。</p>`}
function pickDoor(i){s.picked=i;const opts=[0,1,2].filter(x=>x!==i&&x!==s.prize);s.opened=opts[Math.floor(Math.random()*opts.length)];$('#play').innerHTML=`<span class="trial">STAY OR SWITCH?</span><h2>ハズレが1つ、開きました。</h2><div class="door-row">${[0,1,2].map(x=>`<div class="door ${x===i?'selected':''} ${x===s.opened?'open':''}" style="display:grid;place-items:center">${x===s.opened?'ハズレ':x+1}</div>`).join('')}</div><p>あなたは扉${i+1}を選択中。<br>残ったもう1つの扉に変えますか？</p>${actions(btn('そのまま','monty-stay','secondary')+btn('扉を変える','monty-switch'))}`}
function montyAnswer(change){let chosen=change?[0,1,2].find(x=>x!==s.picked&&x!==s.opened):s.picked;$('#play').innerHTML=`<span class="trial">${chosen===s.prize?'YOU FOUND IT!':'TRY ANOTHER PERSPECTIVE'}</span><h2>${chosen===s.prize?'当たり！':'今回は、ハズレ。'}</h2><p>当たりは扉${s.prize+1}でした。<br>1回の結果だけでは分からない。たくさん試したら？</p>${actions(btn('1,000回シミュレーション','monty-sim'))}<div id="sim" aria-live="polite"></div>`;reveal('変えると、当たる確率は2倍。','最初に当たりを選ぶ確率は1/3。そのままで勝つのは、この場合だけ。最初にハズレを選ぶ確率は2/3で、この場合は扉を変えると勝ちます。案内役が必ずハズレを開け、必ず変更を提案するというルールが大切です。','「2択だから半々」と思ったら、選択肢がどう減ったかも確認しよう。シミュレーションでは毎回ランダムに当たりを決め、両方の作戦を同じ条件で比べます。','monty')}
function simulate(){let stay=0;for(let i=0;i<1000;i++){const prize=Math.floor(Math.random()*3),pick=Math.floor(Math.random()*3);if(prize===pick)stay++}$('#sim').innerHTML=[['そのまま',stay],['変える',1000-stay]].map(([n,v])=>`<div class="bar-row"><span>${n}</span><div class="bar"><i style="width:${v/10}%"></i></div><b>${v}回</b></div>`).join('')+'<p>1,000回中の当たり回数（乱数による模擬実験）</p>'}
function lineSVG(n,hide=false){const l=180-n/2,r=180+n/2;return `<svg class="illusion-svg" viewBox="0 0 360 180" aria-label="長さを比較する2本の横線"><g fill="none" stroke="currentColor" stroke-width="3"><path d="M90 50H270 M${l} 130H${r}"/><g opacity="${hide?0:1}"><path d="M110 30L90 50 110 70 M250 30L270 50 250 70 M${l-20} 110L${l} 130 ${l-20} 150 M${r+20} 110L${r} 130 ${r+20} 150"/></g>${hide?'<path d="M90 22V158 M270 22V158" stroke-dasharray="4 5" stroke-width="1"/>':''}</g></svg>`}
function lineStart(){s.length=150;s.hidden=false;$('#play').innerHTML=`<span class="trial">TRUST YOUR EYES?</span><h2>2本を、同じ長さにしよう。</h2><div id="line-art">${lineSVG(s.length)}</div><label for="length">下の横線の長さ</label><input id="length" class="slider" type="range" min="100" max="250" value="150" step="1">${actions(btn('これで同じ！','line-answer'))}`}
function lineAnswer(){let diff=Math.round((s.length-180)/180*100);s.hidden=true;$('#line-art').innerHTML=lineSVG(s.length,true);$('#play h2').textContent=diff===0?'ぴったり！ 同じ長さです。':`下の線は約${Math.abs(diff)}%${diff>0?'長い':'短い'}。`;$('#length').disabled=true;$('#play .actions').innerHTML=btn('矢羽を切り替える','line-toggle','secondary');reveal('長さの感覚にも、文脈がある。','線の端についた矢羽の向きによって、同じ長さの線が違って見えることがあります。「ミュラー・リヤー錯視」と呼ばれます。矢羽を消すと、横線そのものを比べやすくなります。感じ方の強さには個人差があります。','見た目で寸法を決めるときは、周りの形にも注意。大切な長さは、感覚に加えて定規や数値で確認しよう。','lines')}
function showPass(){$('#modalbody').innerHTML=`<div class="eyebrow">YOUR EXPLORER PASS</div><h2>今日の発見、${completed.size}つ。</h2><p>体験すると、スタンプが集まります。<br>全問正解じゃなくていい。驚いたら、それが収穫。</p><div class="stamp-grid">${exhibits.map((e,i)=>`<div class="stamp ${completed.has(i)?'done':''}"><b>${completed.has(i)?'✳':'0'+(i+1)}</b><span>${e.category}</span></div>`).join('')}</div><p>${completed.size===6?'全展示を探検しました！ 今度は仕掛けを知ったうえで、もう一周してみよう。':'気になる展示から、次の発見へ。'}</p><p class="source">スタンプはこのページを開いている間だけ記録されます。</p>`;$('#modal').showModal()}
document.addEventListener('click',e=>{const r=e.target.closest('[data-room]');if(r){room(Number(r.dataset.room));return}const b=e.target.closest('[data-act]');if(!b)return;let a=b.dataset.act,v=Number(b.dataset.value);if(a==='home')home();else if(a==='retry')room(current);else if(a==='next')current===5?home():room(current+1);else if(a.startsWith('color-'))a==='color-toggle'?$('.color-field').classList.toggle('unmask'):colorAnswer(a.slice(6));else if(a==='stroop-go')stroopGo();else if(a==='stroop-answer')stroopAnswer(v);else if(a==='memory-go')memoryGo();else if(a==='memory-group')memoryGo(true);else if(a==='memory-check')memoryCheck();else if(a==='frame-flip'){s.flip=!s.flip;$('#frame-flip').textContent=s.flip?'A：当たらない20% ／ B：当たる80%':'A：当たる80% ／ B：当たらない20%'}else if(a.startsWith('frame-'))frameAnswer(a.slice(6));else if(a==='door')pickDoor(v);else if(a==='monty-stay'||a==='monty-switch')montyAnswer(a==='monty-switch');else if(a==='monty-sim')simulate();else if(a==='line-answer')lineAnswer();else if(a==='line-toggle'){s.hidden=!s.hidden;$('#line-art').innerHTML=lineSVG(s.length,s.hidden)}});
document.addEventListener('input',e=>{if(e.target.id==='length'){s.length=Number(e.target.value);$('#line-art').innerHTML=lineSVG(s.length)}});document.addEventListener('keydown',e=>{if(e.key==='Enter'&&e.target.id==='memory-input')memoryCheck()});$('.logo').addEventListener('click',e=>{e.preventDefault();home()});$('#passport').onclick=showPass;$('.close').onclick=()=>$('#modal').close();$('#modal').onclick=e=>{if(e.target===$('#modal'))$('#modal').close()};$('#about').onclick=()=>{$('#modalbody').innerHTML='<div class="eyebrow">ABOUT MIND PARK</div><h2>正解より、発見を。</h2><p>見る・注意する・記憶する・選ぶ。身近な心の働きを、短い体験で楽しむ学習ミュージアムです。</p><p>展示は研究や科学教育資料を参考にした学習用のデモです。厳密な心理実験や医療・性格診断ではありません。結果には端末や練習、偶然の影響があります。各展示の解説に参考資料を掲載しています。</p><p>回答や反応時間は外部に送信せず、このページ内で処理します。再読み込みすると結果はリセットされます。</p><button class="secondary" id="motion">演出を控えめに切り替える</button>';$('#modal').showModal();$('#motion').onclick=()=>{document.body.classList.toggle('reduced');$('#motion').textContent=document.body.classList.contains('reduced')?'通常の演出に戻す':'演出を控えめに切り替える'}};home();
